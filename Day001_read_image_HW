import cv2

img_path = 'data/lena.png'
img = cv2.imread(i, cv2.IMREAD_COLOR)
img_gray = cv2.imread(, cv2.IMREAD_GRAYSCALE)
while True:
    
    cv2.imshow('bgr', img)
   
    cv2.imshow('gray', img_gray)
    k = cv2.waitKey(0)
    if k == 27:
        cv2.destroyAllWindows()
        break
